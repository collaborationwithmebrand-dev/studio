rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // WARNING: These rules are for development only and allow anyone to read/write.
      allow read, write: if true;
    }
  }
}