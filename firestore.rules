
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development mode: Allow public read/write access to all collections
    // This supports the "No Login" flow where anyone can browse and order.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
